apiVersion: v1
kind: Service
metadata:
  name: {{ include "hellogitops.fullname" . }}
  labels:
    {{- include "hellogitops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hellogitops.selectorLabels" . | nindent 4 }}
